# Dependencies
node_modules/
.pnp/
.yarn/

# Build output
dist/
*.tsbuildinfo

# Compact compiler artifacts (generated by `npm run compact:build`)
contracts/managed/
deploy/managed/

# Deploy run logs (wallet addresses / sync traces)
deploy/logs/

# Local wallet / secrets — NEVER commit these
.env
.env.local
*.seed
*.key
wallet-*.json

# Editor / OS
.DS_Store
.idea/
.vscode/

# Deploy page: installed SDK + the compiled circuits it serves
deploy-lace/node_modules/
deploy-lace/public/managed/
deploy-lace/dist/

# Wallet / contract private-state stores written at deploy time.
# Holds the contract maintenance authority signing key — never commit.
deploy/midnight-level-db/
**/midnight-level-db/
